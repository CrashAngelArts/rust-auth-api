# Configurações do servidor
SERVER_HOST=127.0.0.1
SERVER_PORT=8080
RUST_LOG=info

# Configurações do banco de dados
DATABASE_URL=./data/auth.db

# Configurações de JWT
JWT_SECRET=seu_segredo_super_secreto_aqui_mude_em_producao
JWT_EXPIRATION=24h  # Tempo de expiração do token em horas
JWT_REFRESH_EXPIRATION=168h  # Tempo de expiração do refresh token (7 dias)

# Flag para habilitar/desabilitar serviço de email
EMAIL_ENABLED=false

# Configurações de email (Gmail)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_USERNAME=crashangelarts@gmail.com
EMAIL_PASSWORD=sua_senha_de_app_aqui
EMAIL_FROM=crashangelarts@gmail.com
EMAIL_FROM_NAME=API de Autenticação
EMAIL_BASE_URL=http://localhost:3000  # URL base para links em emails

# Configurações de segurança
PASSWORD_SALT_ROUNDS=10
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_DURATION=60  # em segundos

# Novas configurações de segurança
USE_ARGON2=true  # Usar Argon2 em vez de bcrypt para hash de senhas
CSRF_ENABLED=true  # Habilitar proteção CSRF
SECURE_HEADERS_ENABLED=true  # Habilitar cabeçalhos de segurança HTTP
COOKIE_SECURE=false  # Cookies com flag Secure (true em produção)
COOKIE_SAME_SITE=Lax  # Política SameSite para cookies (Lax, Strict, None)

# Configurações de 2FA
TWO_FACTOR_ISSUER="Rust Auth API 🔒"  # Nome do emissor para aplicativos 2FA
TWO_FACTOR_DIGITS=6  # Número de dígitos para códigos TOTP
TWO_FACTOR_STEP=30  # Intervalo de tempo em segundos para códigos TOTP

# Configurações de rotação de tokens
TOKEN_ROTATION_ENABLED=true  # Habilitar rotação de tokens
TOKEN_CLEANUP_INTERVAL=86400  # Intervalo para limpeza de tokens expirados (em segundos)

# Configurações de CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Configurações do administrador padrão (usado apenas na primeira execução)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin@123
ADMIN_NAME=Administrador

# Configurações de keystroke dynamics 🔒
SECURITY_KEYSTROKE_THRESHOLD=70  # Limiar de similaridade para verificação (0-100)
SECURITY_RATE_LIMIT_REQUESTS=5   # Número máximo de tentativas de verificação
SECURITY_RATE_LIMIT_DURATION=60  # Duração da janela de rate limiting em segundos
SECURITY_BLOCK_DURATION=300      # Duração do bloqueio após exceder o limite (5 minutos)
